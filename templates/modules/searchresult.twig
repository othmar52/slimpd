{% set startnum = paginator_params.currentPage*paginator_params.itemsPerPage+1-paginator_params.itemsPerPage %}

{% if paginator_params.currentPage == paginator_params.numPages %}
	{% set endnum = paginator_params.totalItems %}
{% else %}
	{% set endnum = paginator_params.currentPage*paginator_params.itemsPerPage %}
{% endif %}
{{ dump(paginator_params)}}
<h2>Searchresults &quot;{{ search.term }}&quot;<span style="margin-left: 1em; font-size: 0.4em; color:#888;">
{{ startnum }} - {{ endnum }} of {{ search.total }} results in {{ search.time }}s</span></h2>

{% if tracklist|length > 0 %}

{% include 'modules/paginator.twig' with { 'paginator': paginator_params, 'll': ll } %}

<div class="tracks_container">
<br />
	{% for item in tracklist %}
		{% include 'partials/trackrow-1.twig' %}
	{% endfor %}
	<div class="clearfix"> </div>
	
</div>
{% include 'modules/paginator.twig' with { 'paginator': paginator_params, 'll': ll } %}
{% else %}
<h3>nothing found...</h3>
{% endif %}