{# no indentation because it will be insertet in input fields #}
{% if labelIds %}
{% set itemIds = labelIds|split(',') %}
{% if itemIds|length > 1 %}
{% for itemId in itemIds %}
{{renderitems.labels[itemId].title}}
{% if loop.last == false %},{% endif %}
{% endfor%}
{% else %}
{{renderitems.labels[labelIds].title}}
{% endif %}
{% endif %}