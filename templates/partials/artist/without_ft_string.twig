{# no indentation because compiled string will be insertet into input fields #}
{% if item.artistId %}
{% set itemIds = item.artistId|split(',') %}
{% for itemId in itemIds %}
{% include 'partials/artist/single-string.twig' %}
{% if loop.last == false %} & {% endif %}
{% endfor %}
{% endif %}